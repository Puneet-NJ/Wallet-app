{
	"name": "bank-webhook",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"start": "npx nodemon dist/index.js",
		"ts.check": "tsc --project tsconfig.json",
		"add-build": "git add dist",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "rimraf dist && esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js"
	},
	"pre-commit": [
		"ts.check",
		"build",
		"add-build"
	],
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@repo/zod": "*",
		"@types/cors": "^2.8.17",
		"@types/express": "^4.17.21",
		"@types/jsonwebtoken": "^9.0.6",
		"cors": "^2.8.5",
		"esbuild": "^0.23.1",
		"express": "^4.21.0",
		"jsonwebtoken": "^9.0.2",
		"zod": "^3.23.8"
	},
	"devDependencies": {
		"pre-commit": "^1.2.2",
		"rimraf": "^6.0.1"
	}
}
